
export enum Mime {
    none = 0,
    page = 10,
    // blog = 11,
    pages = 20,
    //favourite = 30,
    // trash = 40,
    // import = 50,
    // inbox = 60,
    // tables = 70,
    table = 71,
    // files = 80,
    //folder = 81,
    // template = 90,
    chatroom = 100
}


